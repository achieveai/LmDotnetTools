{
  "_comment": "S2S bring-up profile. Select with `CodeReviewDaemon --review s2s`. Runs the review INSIDE a separate LmStreaming.Sample review host (http://localhost:5051) over the S2S REST API instead of an in-process agent loop, so the posted comment can deep-link the real conversation + its code-reviewer:* sub-agent tree. Deliberately isolated from the live poster profile (appsettings.achieveai.json): its own port, DB, log and conversation store, so the two never share state. SECRET: LmStreamingS2SSecret is NOT stored here — pass it as env CodeReviewDaemon__LmStreamingS2SSecret, matching the review host's Auth__S2SInboundSecret. Shared-gateway invariant: SandboxGateway:WorkspaceBasePath must equal the gateway's WORKSPACE_BASE_HOST_PATH, and PerAppWorkspaceRooting+AppId must match what the daemon forwards as X-Sbx-App-Id, so the leaf the preparer clones into is the leaf the gateway mounts. Workspace isolation: each review leases a pooled slot whose directory is a SINGLE segment under the daemon's effective app base ({base}/review-slot-{i}) — LmStreaming's workspace-directory sanitizer strips path separators, so a nested name would silently resolve to a different, empty directory. That leaf mounts at /workspace, which is why the hosted agent sees /workspace/store, /workspace/store/repos/<Repo> and /workspace/store/PRs/<slug>-<pr> exactly as the pooled review stage computes them.",
  "Urls": "http://127.0.0.1:5081",
  "Auth": {
    "TokenStoreDir": "B:/sandbox-workspaces/crd-oauth-tokens",
    "Github": {
      "_comment": "GitHub CLI (gh) first-party OAuth App id + secret (public, safe to embed — same convention as appsettings.achieveai.json). Lets the auth-webhook inject the stored token for the review agent's api.github.com calls.",
      "ClientId": "178c6fc778ccc68e1d6a",
      "ClientSecret": "34ddeff2b558a23d38fba8a6de74f086ede1cc0b",
      "Scopes": [
        "repo",
        "read:org"
      ]
    },
    "Webhook": {
      "PublicBaseUrl": "http://127.0.0.1:5081"
    }
  },
  "SandboxGateway": {
    "BaseUrl": "http://127.0.0.1:3000",
    "AppId": "code-review-daemon",
    "WorkspaceBasePath": "B:/sandbox-workspaces/workspaces"
  },
  "CodeReviewDaemon": {
    "UseS2SReviewAgent": true,
    "LmStreamingBaseUrl": "http://localhost:5051",
    "_LmStreamingProviderId_comment": "Provision carries no model field — the model is whatever this LmStreaming provider resolves. gpt-5.6-luna mirrors the achieveai profile's ReviewModelId and is a Copilot-backed (non-CLI) provider, which workspace-agent mode requires.",
    "LmStreamingProviderId": "gpt-5.6-luna",
    "LmStreamingReviewMarketplace": "gb-plugins",
    "_EnableToolAssistedReview_comment": "ON, but it means something different here. On the S2S path 'tool-assisted' does NOT mean the daemon provisions its own review sandbox — it does not; the tools, MCP transport and code-reviewer:* sub-agents all live inside the LmStreaming-hosted conversation. What the flag buys is the POOLED path: with EnableReviewerWrites + a resolved store it leases a warm ReviewSlot, prepares it host-side (store + KnowledgeBase + the PR's notes branch + the PR head checked out), and that leased slot is the directory mounted into the hosted conversation at /workspace. That is what makes notes, per-PR memory and KB grounding real on S2S. The flag name is shared by all three profiles, so it keeps its original name and this comment carries the truth. NOTE: the RequireSkillSupport fail-fast lives in the daemon's own session provisioning and therefore does NOT apply here.",
    "EnableToolAssistedReview": true,
    "_EnableReviewerWrites_comment": "The reviewer writes PR_Context_NN.md / PR_Findings_NN.md into its notes dir. On S2S the in-process ScopedToolFilter cannot wrap the hosted agent's Write/Edit, so the enforcement boundary is the COMMIT GATE: the daemon stages only the PR's notes dir, the write credential never enters the agent session, and anything written elsewhere in the slot is erased by SlotHygiene's strip + clean-on-entry.",
    "EnableReviewerWrites": true,
    "EnableKnowledgeAgent": true,
    "_EnableJudgeAgent_comment": "The judge runs as a SEPARATE hosted conversation against the same slot workspace (so it shares the slot's container, not the review's thread). EnableABVariants stays off: a second full review arm doubles cost for a comparison this bring-up does not need.",
    "EnableJudgeAgent": true,
    "EnableABVariants": false,
    "EnableCommentPosting": false,
    "PerAppWorkspaceRooting": true,
    "EnabledRepos": [
      "achieveai/LmDotnetTools"
    ],
    "_store_comment": "The cross-repo store the pooled slots clone. Its submodule repos/<Repo> is the reviewed checkout, Contracts/ the shared layer, and KnowledgeBase/ what the review reads and the at-close extraction writes. Without this the pooled path returns false and S2S falls back to the bare per-PR clone.",
    "CrossRepoStoreUrl": "https://github.com/achieveai/AchieveAiReviews",
    "ReviewBotRepoUrl": "https://github.com/achieveai/AchieveAiReviews",
    "_ReviewPoolSize_comment": "One single-segment workspace leaf per slot ({base}/review-slot-{i}), each mounted as its own LmStreaming workspace ⇒ its own gateway container. Two concurrent reviews holding different leases share nothing. The poller is still serial, so this is warm-slot reuse today and the isolation that makes parallel a poller-only change later.",
    "ReviewPoolSize": 2,
    "ScratchDirName": "scratch",
    "KnowledgeModelId": "claude-opus-4.8",
    "_MaxPrAgeDays_comment": "Scopes this bring-up to the single recent PR under test (#222); the other open PRs are weeks older and fall outside the window.",
    "MaxPrAgeDays": 2,
    "DatabasePath": "B:/sources/LmDotnetTools/.run/s2s-review.db",
    "LogFilePath": "B:/sources/LmDotnetTools/.run/s2s-daemon.jsonl",
    "ConversationStorePath": "B:/sources/LmDotnetTools/.run/s2s-conversations",
    "Marketplaces": [
      "gb-plugins"
    ],
    "SubAgentMarketplaces": [
      "gb-plugins"
    ],
    "ReviewModelId": "gpt-5.6-luna",
    "ReviewMaxTurns": 150,
    "BotName": "Revobot"
  }
}
